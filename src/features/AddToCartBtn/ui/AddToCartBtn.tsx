import {Button} from "@/shared/ui";

const AddToCartBtn = () => {
    return (
        <Button className={`cursor-pointer`}>
            Добавить в корзину
        </Button>
    );
};

export default AddToCartBtn;